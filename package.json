{
  "name": "chat-app",
  "version": "0.0.1",
  "description": "example of chat app baset on websockets - socket.io",
  "main": "index.js",
  "scripts": {
    "postinstall": "bower install --config.interactive=false",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gcloud app deploy --version head"
  },
  "engines": {
    "node": "~7.0"
  },
  "keywords": [
    "socket.io",
    "chat"
  ],
  "author": "matikrk",
  "license": "ISC",
  "dependencies": {
    "bower": "^1.8.0",
    "express": "^4.14.0",
    "socket.io": "^1.5.1"
  }
}
